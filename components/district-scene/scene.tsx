"use client";

// The 3D district (React Three Fiber on WebGL 2) will live here. The page
// never imports this module directly; it only reaches it through the lazy,
// client-only boundary in ./index, which tests swap out.
export default function Scene() {
  return null;
}
